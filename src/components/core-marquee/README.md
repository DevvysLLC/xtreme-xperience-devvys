# Core Marquee Component

A marquee component that renders a scrolling text display from comma-separated values.

## Props

- `text: string` - Comma-separated string of text items to display

## Example

```tsx
import { CoreMarquee } from '@/components/core-marquee'

;<CoreMarquee text="Item 1, Item 2, Item 3" />
```
