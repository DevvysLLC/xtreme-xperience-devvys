query sitemapPosts($first: IntType!, $skip: IntType!) {
  allPosts(first: $first, skip: $skip, orderBy: _updatedAt_DESC) {
    _updatedAt
    config {
      handle
      seo {
        noIndex
      }
    }
  }
}
